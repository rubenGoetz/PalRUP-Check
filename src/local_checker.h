
#pragma once

#include <stdbool.h>

void local_checker_init(const char*, const char*, const char*, unsigned long, unsigned long, unsigned long, unsigned long, bool);
void local_checker_end();
int local_checker_run();
